import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF0D1117); // Dark blue/black for dark theme background
const kAccentColor = Color(0xFF00BFFF); // Bright blue, used as primary accent
const kSecondaryAccentColor = Color(0xFFF021B7); // Pink/magenta, used as secondary accent
const kLightTextColor = Colors.white; // Text color for dark theme
const kDarkTextColor = Color(0xff25292B); // Text color for light theme

const Bold = 'Bold'; // Font family name
const Regular = 'Regular'; // Font family name

// Backgrounds
const Color kDarkBackgroundColor = Color(0xFF0D1117);      // Elegant deep blue-black
const Color kLightBackgroundColor = Color(0xFFF2F4F6);     // Soft gray, easy on eyes
const Color kLightAltBackgroundColor = Color(0xFFFAFAFA);  // Slightly brighter
